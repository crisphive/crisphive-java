/*
 * Crisphive Developer API
 * Public REST API for integrating Crisphive from your own backend. Authenticate every request with a secret API key as a Bearer token (`Authorization: Bearer chsk_live_…`). The key prefix selects the data environment: `chsk_live_…` → production (live), `chsk_test_…` → sandbox (isolated test).  **Key scopes (restricted keys).** A key is either *full-access* (can call every endpoint below) or *restricted* to a set of permission codes chosen at creation — the same codes as the dashboard permission grid (e.g. `customers_view`, `job_create`, `team_manage`). A restricted key calling an endpoint outside its scope gets `403`. The full code list is the permission catalog (`GET /permission/modules` on the dashboard API). Create, scope, and revoke keys from the business dashboard.  Every response is wrapped in the envelope `{ \"error_code\": 0, \"message\": \"Success\", \"data\": <payload> }`.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.crisphive.client.model;

import java.util.Objects;
import com.crisphive.client.model.JobRequestTechCandidate;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.UUID;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.crisphive.client.JSON;

/**
 * JobRequestBuddySlotCandidates
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.11.0")
public class JobRequestBuddySlotCandidates {
  public static final String SERIALIZED_NAME_CANDIDATES = "candidates";
  @SerializedName(SERIALIZED_NAME_CANDIDATES)
  @javax.annotation.Nullable
  private List<JobRequestTechCandidate> candidates = new ArrayList<>();

  public static final String SERIALIZED_NAME_SKILL_IDS = "skill_ids";
  @SerializedName(SERIALIZED_NAME_SKILL_IDS)
  @javax.annotation.Nullable
  private List<UUID> skillIds = new ArrayList<>();

  public static final String SERIALIZED_NAME_SLOT = "slot";
  @SerializedName(SERIALIZED_NAME_SLOT)
  @javax.annotation.Nullable
  private Integer slot;

  public JobRequestBuddySlotCandidates() {
  }

  public JobRequestBuddySlotCandidates candidates(@javax.annotation.Nullable List<JobRequestTechCandidate> candidates) {
    this.candidates = candidates;
    return this;
  }

  public JobRequestBuddySlotCandidates addCandidatesItem(JobRequestTechCandidate candidatesItem) {
    if (this.candidates == null) {
      this.candidates = new ArrayList<>();
    }
    this.candidates.add(candidatesItem);
    return this;
  }

  /**
   * Ranked feasible technicians for this slot, best total_score first.
   * @return candidates
   */
  @javax.annotation.Nullable
  public List<JobRequestTechCandidate> getCandidates() {
    return candidates;
  }

  public void setCandidates(@javax.annotation.Nullable List<JobRequestTechCandidate> candidates) {
    this.candidates = candidates;
  }


  public JobRequestBuddySlotCandidates skillIds(@javax.annotation.Nullable List<UUID> skillIds) {
    this.skillIds = skillIds;
    return this;
  }

  public JobRequestBuddySlotCandidates addSkillIdsItem(UUID skillIdsItem) {
    if (this.skillIds == null) {
      this.skillIds = new ArrayList<>();
    }
    this.skillIds.add(skillIdsItem);
    return this;
  }

  /**
   * Skill UUIDs this buddy slot was quoted to require. Omitted when the slot has no skill requirement.
   * @return skillIds
   */
  @javax.annotation.Nullable
  public List<UUID> getSkillIds() {
    return skillIds;
  }

  public void setSkillIds(@javax.annotation.Nullable List<UUID> skillIds) {
    this.skillIds = skillIds;
  }


  public JobRequestBuddySlotCandidates slot(@javax.annotation.Nullable Integer slot) {
    this.slot = slot;
    return this;
  }

  /**
   * Zero-based index of this buddy slot in the crew plan.
   * @return slot
   */
  @javax.annotation.Nullable
  public Integer getSlot() {
    return slot;
  }

  public void setSlot(@javax.annotation.Nullable Integer slot) {
    this.slot = slot;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    JobRequestBuddySlotCandidates jobRequestBuddySlotCandidates = (JobRequestBuddySlotCandidates) o;
    return Objects.equals(this.candidates, jobRequestBuddySlotCandidates.candidates) &&
        Objects.equals(this.skillIds, jobRequestBuddySlotCandidates.skillIds) &&
        Objects.equals(this.slot, jobRequestBuddySlotCandidates.slot);
  }

  @Override
  public int hashCode() {
    return Objects.hash(candidates, skillIds, slot);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class JobRequestBuddySlotCandidates {\n");
    sb.append("    candidates: ").append(toIndentedString(candidates)).append("\n");
    sb.append("    skillIds: ").append(toIndentedString(skillIds)).append("\n");
    sb.append("    slot: ").append(toIndentedString(slot)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("candidates");
    openapiFields.add("skill_ids");
    openapiFields.add("slot");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to JobRequestBuddySlotCandidates
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!JobRequestBuddySlotCandidates.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in JobRequestBuddySlotCandidates is not found in the empty JSON string", JobRequestBuddySlotCandidates.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!JobRequestBuddySlotCandidates.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `JobRequestBuddySlotCandidates` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("candidates") != null && !jsonObj.get("candidates").isJsonNull()) {
        JsonArray jsonArraycandidates = jsonObj.getAsJsonArray("candidates");
        if (jsonArraycandidates != null) {
          // ensure the json data is an array
          if (!jsonObj.get("candidates").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `candidates` to be an array in the JSON string but got `%s`", jsonObj.get("candidates").toString()));
          }

          // validate the optional field `candidates` (array)
          for (int i = 0; i < jsonArraycandidates.size(); i++) {
            JobRequestTechCandidate.validateJsonElement(jsonArraycandidates.get(i));
          };
        }
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("skill_ids") != null && !jsonObj.get("skill_ids").isJsonNull() && !jsonObj.get("skill_ids").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `skill_ids` to be an array in the JSON string but got `%s`", jsonObj.get("skill_ids").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!JobRequestBuddySlotCandidates.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'JobRequestBuddySlotCandidates' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<JobRequestBuddySlotCandidates> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(JobRequestBuddySlotCandidates.class));

       return (TypeAdapter<T>) new TypeAdapter<JobRequestBuddySlotCandidates>() {
           @Override
           public void write(JsonWriter out, JobRequestBuddySlotCandidates value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public JobRequestBuddySlotCandidates read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of JobRequestBuddySlotCandidates given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of JobRequestBuddySlotCandidates
   * @throws IOException if the JSON string is invalid with respect to JobRequestBuddySlotCandidates
   */
  public static JobRequestBuddySlotCandidates fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, JobRequestBuddySlotCandidates.class);
  }

  /**
   * Convert an instance of JobRequestBuddySlotCandidates to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

