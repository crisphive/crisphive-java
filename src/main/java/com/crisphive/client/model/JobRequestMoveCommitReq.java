/*
 * Crisphive Developer API
 * Public REST API for integrating Crisphive from your own backend. Authenticate every request with a secret API key as a Bearer token (`Authorization: Bearer chsk_live_…`). The key prefix selects the data environment: `chsk_live_…` → production (live), `chsk_test_…` → sandbox (isolated test).  **Key scopes (restricted keys).** A key is either *full-access* (can call every endpoint below) or *restricted* to a set of permission codes chosen at creation — the same codes as the dashboard permission grid (e.g. `customers_view`, `job_create`, `team_manage`). A restricted key calling an endpoint outside its scope gets `403`. The full code list is the permission catalog (`GET /permission/modules` on the dashboard API). Create, scope, and revoke keys from the business dashboard.  Every response is wrapped in the envelope `{ \"error_code\": 0, \"message\": \"Success\", \"data\": <payload> }`.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.crisphive.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.UUID;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.crisphive.client.JSON;

/**
 * JobRequestMoveCommitReq
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.11.0")
public class JobRequestMoveCommitReq {
  public static final String SERIALIZED_NAME_EXPECTED_MEMBER_IDS = "expected_member_ids";
  @SerializedName(SERIALIZED_NAME_EXPECTED_MEMBER_IDS)
  @javax.annotation.Nullable
  private List<UUID> expectedMemberIds = new ArrayList<>();

  public static final String SERIALIZED_NAME_EXPECTED_MOVE_IDS = "expected_move_ids";
  @SerializedName(SERIALIZED_NAME_EXPECTED_MOVE_IDS)
  @javax.annotation.Nullable
  private List<UUID> expectedMoveIds = new ArrayList<>();

  public static final String SERIALIZED_NAME_EXPECTED_VERSION = "expected_version";
  @SerializedName(SERIALIZED_NAME_EXPECTED_VERSION)
  @javax.annotation.Nullable
  private Integer expectedVersion;

  /**
   * Cascade mode: overtime | next_day (must match the preview).
   */
  @JsonAdapter(ModeEnum.Adapter.class)
  public enum ModeEnum {
    OVERTIME("overtime"),
    
    NEXT_DAY("next_day");

    private String value;

    ModeEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static ModeEnum fromValue(String value) {
      for (ModeEnum b : ModeEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      throw new IllegalArgumentException("Unexpected value '" + value + "'");
    }

    public static class Adapter extends TypeAdapter<ModeEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final ModeEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public ModeEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return ModeEnum.fromValue(value);
      }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      String value = jsonElement.getAsString();
      ModeEnum.fromValue(value);
    }
  }

  public static final String SERIALIZED_NAME_MODE = "mode";
  @SerializedName(SERIALIZED_NAME_MODE)
  @javax.annotation.Nonnull
  private ModeEnum mode;

  public static final String SERIALIZED_NAME_START_AT = "start_at";
  @SerializedName(SERIALIZED_NAME_START_AT)
  @javax.annotation.Nonnull
  private String startAt;

  public static final String SERIALIZED_NAME_TECHNICIAN_ID = "technician_id";
  @SerializedName(SERIALIZED_NAME_TECHNICIAN_ID)
  @javax.annotation.Nonnull
  private UUID technicianId;

  public JobRequestMoveCommitReq() {
  }

  public JobRequestMoveCommitReq expectedMemberIds(@javax.annotation.Nullable List<UUID> expectedMemberIds) {
    this.expectedMemberIds = expectedMemberIds;
    return this;
  }

  public JobRequestMoveCommitReq addExpectedMemberIdsItem(UUID expectedMemberIdsItem) {
    if (this.expectedMemberIds == null) {
      this.expectedMemberIds = new ArrayList<>();
    }
    this.expectedMemberIds.add(expectedMemberIdsItem);
    return this;
  }

  /**
   * ExpectedMemberIDs — crew moves only: the FULL member set the preview staffed (echo data.members[].technician_id). If the commit&#39;s re-plan would staff a DIFFERENT set (a previewed replacement got booked in the meantime), the commit is rejected with SCHEDULE_MOVE_PLAN_DRIFTED — crew swaps are never approved unseen. Omit to opt out.
   * @return expectedMemberIds
   */
  @javax.annotation.Nullable
  public List<UUID> getExpectedMemberIds() {
    return expectedMemberIds;
  }

  public void setExpectedMemberIds(@javax.annotation.Nullable List<UUID> expectedMemberIds) {
    this.expectedMemberIds = expectedMemberIds;
  }


  public JobRequestMoveCommitReq expectedMoveIds(@javax.annotation.Nullable List<UUID> expectedMoveIds) {
    this.expectedMoveIds = expectedMoveIds;
    return this;
  }

  public JobRequestMoveCommitReq addExpectedMoveIdsItem(UUID expectedMoveIdsItem) {
    if (this.expectedMoveIds == null) {
      this.expectedMoveIds = new ArrayList<>();
    }
    this.expectedMoveIds.add(expectedMoveIdsItem);
    return this;
  }

  /**
   * ExpectedMoveIDs — the displaced job IDs the preview returned (echo data.days[].moves[].job_id). Optional but RECOMMENDED: if the schedule changed so the commit would push a different set (e.g. a job booked onto the target tech since the preview), the commit is rejected with SCHEDULE_MOVE_PLAN_DRIFTED instead of silently pushing an unseen job.
   * @return expectedMoveIds
   */
  @javax.annotation.Nullable
  public List<UUID> getExpectedMoveIds() {
    return expectedMoveIds;
  }

  public void setExpectedMoveIds(@javax.annotation.Nullable List<UUID> expectedMoveIds) {
    this.expectedMoveIds = expectedMoveIds;
  }


  public JobRequestMoveCommitReq expectedVersion(@javax.annotation.Nullable Integer expectedVersion) {
    this.expectedVersion = expectedVersion;
    return this;
  }

  /**
   * ExpectedVersion — the moved job&#39;s status_version from the preview (0 &#x3D; fence on the fresh plan-read value; negatives rejected).
   * minimum: 0
   * @return expectedVersion
   */
  @javax.annotation.Nullable
  public Integer getExpectedVersion() {
    return expectedVersion;
  }

  public void setExpectedVersion(@javax.annotation.Nullable Integer expectedVersion) {
    this.expectedVersion = expectedVersion;
  }


  public JobRequestMoveCommitReq mode(@javax.annotation.Nonnull ModeEnum mode) {
    this.mode = mode;
    return this;
  }

  /**
   * Cascade mode: overtime | next_day (must match the preview).
   * @return mode
   */
  @javax.annotation.Nonnull
  public ModeEnum getMode() {
    return mode;
  }

  public void setMode(@javax.annotation.Nonnull ModeEnum mode) {
    this.mode = mode;
  }


  public JobRequestMoveCommitReq startAt(@javax.annotation.Nonnull String startAt) {
    this.startAt = startAt;
    return this;
  }

  /**
   * New start — business-local naive datetime, no offset. Must match the preview and be in the future.
   * @return startAt
   */
  @javax.annotation.Nonnull
  public String getStartAt() {
    return startAt;
  }

  public void setStartAt(@javax.annotation.Nonnull String startAt) {
    this.startAt = startAt;
  }


  public JobRequestMoveCommitReq technicianId(@javax.annotation.Nonnull UUID technicianId) {
    this.technicianId = technicianId;
    return this;
  }

  /**
   * Target technician (must match the preview).
   * @return technicianId
   */
  @javax.annotation.Nonnull
  public UUID getTechnicianId() {
    return technicianId;
  }

  public void setTechnicianId(@javax.annotation.Nonnull UUID technicianId) {
    this.technicianId = technicianId;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    JobRequestMoveCommitReq jobRequestMoveCommitReq = (JobRequestMoveCommitReq) o;
    return Objects.equals(this.expectedMemberIds, jobRequestMoveCommitReq.expectedMemberIds) &&
        Objects.equals(this.expectedMoveIds, jobRequestMoveCommitReq.expectedMoveIds) &&
        Objects.equals(this.expectedVersion, jobRequestMoveCommitReq.expectedVersion) &&
        Objects.equals(this.mode, jobRequestMoveCommitReq.mode) &&
        Objects.equals(this.startAt, jobRequestMoveCommitReq.startAt) &&
        Objects.equals(this.technicianId, jobRequestMoveCommitReq.technicianId);
  }

  @Override
  public int hashCode() {
    return Objects.hash(expectedMemberIds, expectedMoveIds, expectedVersion, mode, startAt, technicianId);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class JobRequestMoveCommitReq {\n");
    sb.append("    expectedMemberIds: ").append(toIndentedString(expectedMemberIds)).append("\n");
    sb.append("    expectedMoveIds: ").append(toIndentedString(expectedMoveIds)).append("\n");
    sb.append("    expectedVersion: ").append(toIndentedString(expectedVersion)).append("\n");
    sb.append("    mode: ").append(toIndentedString(mode)).append("\n");
    sb.append("    startAt: ").append(toIndentedString(startAt)).append("\n");
    sb.append("    technicianId: ").append(toIndentedString(technicianId)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("expected_member_ids");
    openapiFields.add("expected_move_ids");
    openapiFields.add("expected_version");
    openapiFields.add("mode");
    openapiFields.add("start_at");
    openapiFields.add("technician_id");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("mode");
    openapiRequiredFields.add("start_at");
    openapiRequiredFields.add("technician_id");
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to JobRequestMoveCommitReq
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!JobRequestMoveCommitReq.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in JobRequestMoveCommitReq is not found in the empty JSON string", JobRequestMoveCommitReq.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!JobRequestMoveCommitReq.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `JobRequestMoveCommitReq` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : JobRequestMoveCommitReq.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("expected_member_ids") != null && !jsonObj.get("expected_member_ids").isJsonNull() && !jsonObj.get("expected_member_ids").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `expected_member_ids` to be an array in the JSON string but got `%s`", jsonObj.get("expected_member_ids").toString()));
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("expected_move_ids") != null && !jsonObj.get("expected_move_ids").isJsonNull() && !jsonObj.get("expected_move_ids").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `expected_move_ids` to be an array in the JSON string but got `%s`", jsonObj.get("expected_move_ids").toString()));
      }
      if (!jsonObj.get("mode").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `mode` to be a primitive type in the JSON string but got `%s`", jsonObj.get("mode").toString()));
      }
      // validate the required field `mode`
      ModeEnum.validateJsonElement(jsonObj.get("mode"));
      if (!jsonObj.get("start_at").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `start_at` to be a primitive type in the JSON string but got `%s`", jsonObj.get("start_at").toString()));
      }
      if (!jsonObj.get("technician_id").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `technician_id` to be a primitive type in the JSON string but got `%s`", jsonObj.get("technician_id").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!JobRequestMoveCommitReq.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'JobRequestMoveCommitReq' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<JobRequestMoveCommitReq> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(JobRequestMoveCommitReq.class));

       return (TypeAdapter<T>) new TypeAdapter<JobRequestMoveCommitReq>() {
           @Override
           public void write(JsonWriter out, JobRequestMoveCommitReq value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public JobRequestMoveCommitReq read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of JobRequestMoveCommitReq given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of JobRequestMoveCommitReq
   * @throws IOException if the JSON string is invalid with respect to JobRequestMoveCommitReq
   */
  public static JobRequestMoveCommitReq fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, JobRequestMoveCommitReq.class);
  }

  /**
   * Convert an instance of JobRequestMoveCommitReq to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

