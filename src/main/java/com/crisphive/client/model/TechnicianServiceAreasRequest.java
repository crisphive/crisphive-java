/*
 * Crisphive Developer API
 * Public REST API for integrating Crisphive from your own backend. Authenticate every request with a secret API key as a Bearer token (`Authorization: Bearer chsk_live_…`). The key prefix selects the data environment: `chsk_live_…` → production (live), `chsk_test_…` → sandbox (isolated test).  **Key scopes (restricted keys).** A key is either *full-access* (can call every endpoint below) or *restricted* to a set of permission codes chosen at creation — the same codes as the dashboard permission grid (e.g. `customers_view`, `job_create`, `team_manage`). A restricted key calling an endpoint outside its scope gets `403`. The full code list is the permission catalog (`GET /permission/modules` on the dashboard API). Create, scope, and revoke keys from the business dashboard.  Every response is wrapped in the envelope `{ \"error_code\": 0, \"message\": \"Success\", \"data\": <payload> }`.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.crisphive.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.UUID;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.crisphive.client.JSON;

/**
 * TechnicianServiceAreasRequest
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.11.0")
public class TechnicianServiceAreasRequest {
  public static final String SERIALIZED_NAME_SERVICE_AREA_IDS = "service_area_ids";
  @SerializedName(SERIALIZED_NAME_SERVICE_AREA_IDS)
  @javax.annotation.Nullable
  private List<UUID> serviceAreaIds = new ArrayList<>();

  public TechnicianServiceAreasRequest() {
  }

  public TechnicianServiceAreasRequest serviceAreaIds(@javax.annotation.Nullable List<UUID> serviceAreaIds) {
    this.serviceAreaIds = serviceAreaIds;
    return this;
  }

  public TechnicianServiceAreasRequest addServiceAreaIdsItem(UUID serviceAreaIdsItem) {
    if (this.serviceAreaIds == null) {
      this.serviceAreaIds = new ArrayList<>();
    }
    this.serviceAreaIds.add(serviceAreaIdsItem);
    return this;
  }

  /**
   * Service-area ids (max 50). Discover via GET /service-areas.
   * @return serviceAreaIds
   */
  @javax.annotation.Nullable
  public List<UUID> getServiceAreaIds() {
    return serviceAreaIds;
  }

  public void setServiceAreaIds(@javax.annotation.Nullable List<UUID> serviceAreaIds) {
    this.serviceAreaIds = serviceAreaIds;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    TechnicianServiceAreasRequest technicianServiceAreasRequest = (TechnicianServiceAreasRequest) o;
    return Objects.equals(this.serviceAreaIds, technicianServiceAreasRequest.serviceAreaIds);
  }

  @Override
  public int hashCode() {
    return Objects.hash(serviceAreaIds);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class TechnicianServiceAreasRequest {\n");
    sb.append("    serviceAreaIds: ").append(toIndentedString(serviceAreaIds)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("service_area_ids");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to TechnicianServiceAreasRequest
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!TechnicianServiceAreasRequest.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in TechnicianServiceAreasRequest is not found in the empty JSON string", TechnicianServiceAreasRequest.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!TechnicianServiceAreasRequest.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `TechnicianServiceAreasRequest` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("service_area_ids") != null && !jsonObj.get("service_area_ids").isJsonNull() && !jsonObj.get("service_area_ids").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `service_area_ids` to be an array in the JSON string but got `%s`", jsonObj.get("service_area_ids").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!TechnicianServiceAreasRequest.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'TechnicianServiceAreasRequest' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<TechnicianServiceAreasRequest> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(TechnicianServiceAreasRequest.class));

       return (TypeAdapter<T>) new TypeAdapter<TechnicianServiceAreasRequest>() {
           @Override
           public void write(JsonWriter out, TechnicianServiceAreasRequest value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public TechnicianServiceAreasRequest read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of TechnicianServiceAreasRequest given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of TechnicianServiceAreasRequest
   * @throws IOException if the JSON string is invalid with respect to TechnicianServiceAreasRequest
   */
  public static TechnicianServiceAreasRequest fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, TechnicianServiceAreasRequest.class);
  }

  /**
   * Convert an instance of TechnicianServiceAreasRequest to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

