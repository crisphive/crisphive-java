/*
 * Crisphive Developer API
 * Public REST API for integrating Crisphive from your own backend. Authenticate every request with a secret API key as a Bearer token (`Authorization: Bearer chsk_live_…`). The key prefix selects the data environment: `chsk_live_…` → production (live), `chsk_test_…` → sandbox (isolated test).  **Key scopes (restricted keys).** A key is either *full-access* (can call every endpoint below) or *restricted* to a set of permission codes chosen at creation — the same codes as the dashboard permission grid (e.g. `customers_view`, `job_create`, `team_manage`). A restricted key calling an endpoint outside its scope gets `403`. The full code list is the permission catalog (`GET /permission/modules` on the dashboard API). Create, scope, and revoke keys from the business dashboard.  Every response is wrapped in the envelope `{ \"error_code\": 0, \"message\": \"Success\", \"data\": <payload> }`.
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.crisphive.client.model;

import java.util.Objects;
import com.crisphive.client.model.JobRequestLeadCandidate;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.UUID;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.crisphive.client.JSON;

/**
 * JobRequestCrewCandidates
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.11.0")
public class JobRequestCrewCandidates {
  public static final String SERIALIZED_NAME_JOB_REQUEST_ID = "job_request_id";
  @SerializedName(SERIALIZED_NAME_JOB_REQUEST_ID)
  @javax.annotation.Nullable
  private UUID jobRequestId;

  public static final String SERIALIZED_NAME_LEADS = "leads";
  @SerializedName(SERIALIZED_NAME_LEADS)
  @javax.annotation.Nullable
  private List<JobRequestLeadCandidate> leads = new ArrayList<>();

  public JobRequestCrewCandidates() {
  }

  public JobRequestCrewCandidates jobRequestId(@javax.annotation.Nullable UUID jobRequestId) {
    this.jobRequestId = jobRequestId;
    return this;
  }

  /**
   * Job identifier — the human-readable short code when present, else the job UUID.
   * @return jobRequestId
   */
  @javax.annotation.Nullable
  public UUID getJobRequestId() {
    return jobRequestId;
  }

  public void setJobRequestId(@javax.annotation.Nullable UUID jobRequestId) {
    this.jobRequestId = jobRequestId;
  }


  public JobRequestCrewCandidates leads(@javax.annotation.Nullable List<JobRequestLeadCandidate> leads) {
    this.leads = leads;
    return this;
  }

  public JobRequestCrewCandidates addLeadsItem(JobRequestLeadCandidate leadsItem) {
    if (this.leads == null) {
      this.leads = new ArrayList<>();
    }
    this.leads.add(leadsItem);
    return this;
  }

  /**
   * Ranked feasible lead technicians, best total_score first; each carries its own buddy/vehicle sections when requested.
   * @return leads
   */
  @javax.annotation.Nullable
  public List<JobRequestLeadCandidate> getLeads() {
    return leads;
  }

  public void setLeads(@javax.annotation.Nullable List<JobRequestLeadCandidate> leads) {
    this.leads = leads;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    JobRequestCrewCandidates jobRequestCrewCandidates = (JobRequestCrewCandidates) o;
    return Objects.equals(this.jobRequestId, jobRequestCrewCandidates.jobRequestId) &&
        Objects.equals(this.leads, jobRequestCrewCandidates.leads);
  }

  @Override
  public int hashCode() {
    return Objects.hash(jobRequestId, leads);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class JobRequestCrewCandidates {\n");
    sb.append("    jobRequestId: ").append(toIndentedString(jobRequestId)).append("\n");
    sb.append("    leads: ").append(toIndentedString(leads)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("job_request_id");
    openapiFields.add("leads");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to JobRequestCrewCandidates
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!JobRequestCrewCandidates.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in JobRequestCrewCandidates is not found in the empty JSON string", JobRequestCrewCandidates.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!JobRequestCrewCandidates.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `JobRequestCrewCandidates` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("job_request_id") != null && !jsonObj.get("job_request_id").isJsonNull()) && !jsonObj.get("job_request_id").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `job_request_id` to be a primitive type in the JSON string but got `%s`", jsonObj.get("job_request_id").toString()));
      }
      if (jsonObj.get("leads") != null && !jsonObj.get("leads").isJsonNull()) {
        JsonArray jsonArrayleads = jsonObj.getAsJsonArray("leads");
        if (jsonArrayleads != null) {
          // ensure the json data is an array
          if (!jsonObj.get("leads").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `leads` to be an array in the JSON string but got `%s`", jsonObj.get("leads").toString()));
          }

          // validate the optional field `leads` (array)
          for (int i = 0; i < jsonArrayleads.size(); i++) {
            JobRequestLeadCandidate.validateJsonElement(jsonArrayleads.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!JobRequestCrewCandidates.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'JobRequestCrewCandidates' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<JobRequestCrewCandidates> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(JobRequestCrewCandidates.class));

       return (TypeAdapter<T>) new TypeAdapter<JobRequestCrewCandidates>() {
           @Override
           public void write(JsonWriter out, JobRequestCrewCandidates value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public JobRequestCrewCandidates read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of JobRequestCrewCandidates given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of JobRequestCrewCandidates
   * @throws IOException if the JSON string is invalid with respect to JobRequestCrewCandidates
   */
  public static JobRequestCrewCandidates fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, JobRequestCrewCandidates.class);
  }

  /**
   * Convert an instance of JobRequestCrewCandidates to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

